//
// Created by 荒井哲也 on 2022/06/18.
//

#include <bits/stdc++.h>
using namespace std;

int main() {
    long long n;
    cin >> n;

    vector<long long> l(n), r(n);
    for (int i = 0; i < n; i++) {
        cin >> l.at(i) >> r.at(i);
    }

    vector<vector<long long>> s(<vector>);
    for (int i = 0; i < n; i++) {
        s.push_back();
    }

    cout << p << endl;
}
